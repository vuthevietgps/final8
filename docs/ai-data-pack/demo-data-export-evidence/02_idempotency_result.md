# Idempotency Result

Status: `not_executed_blocked_missing_safe_throwaway_mongodb_uri`.

Prompt 28 unit coverage still verifies the design-level idempotency properties:

- Apply mode resets deterministic large-profile demo `_id` allowlists before insert.
- Reset operations are `_id` allowlist based.
- Large reset allowlist covers ids generated by smaller profiles.

Prompt 30 did not perform runtime idempotency against MongoDB because no safe throwaway `MONGODB_URI` exists in the environment.

Required future command sequence:

```powershell
$env:NODE_ENV='test'
$env:ALLOW_DEMO_SEED='1'
$env:MONGODB_URI='<SAFE_THROWAWAY_MONGODB_URI>'
npm run seed:ai-data-pack:director:demo -- --apply --profile medium
npm run seed:ai-data-pack:director:demo -- --apply --profile medium
```

Expected future result:

```text
First apply inserts medium profile counts.
Second apply deletes deterministic demo ids first and reinserts medium profile counts.
Counts do not double.
```
