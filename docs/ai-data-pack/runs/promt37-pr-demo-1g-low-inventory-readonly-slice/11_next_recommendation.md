# Next Recommendation

Next recommended slice:

```text
Define reserved/incoming inventory semantics before strengthening low_inventory_best_seller.
```

Concrete next work:

- map reserved quantity from existing order/inventory workflow if available
- map incoming stock only after purchase order status semantics are confirmed
- add a data-quality row explaining when low-inventory evidence is advisory-only
- keep Action Draft Schema, action import, provider execution, OpenAI upload, and Phase 3 parked

