# Risks And Open Questions

Risks:

- reserved is derived, not canonical
- incoming is derived, not confirmed by a separate approval status
- projected coverage can overstate availability if PO delivery slips

Open questions:

- whether to add canonical reserved-stock semantics later
- whether to add PO confirmation/approval semantics later
- whether to persist order fulfillment source

