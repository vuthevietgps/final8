# Implementation Decision

Decision:

```text
implemented_partial_read_only_upgrade
```

Reason:

Existing read-only fields are enough for cautious candidate values. They are not enough for strong inventory/replenishment conclusions.

The row remains advisory-only.

