# Tests And Safety Evidence

Commands run:

```powershell
npm test -- --runTestsByPath src/ai-data-pack/demo-seed/director-demo-seed.spec.ts --runInBand
npm run seed:ai-data-pack:director:demo -- --dry-run --profile small
npm run build
rg -n "fetch\\(|axios|node-fetch|OpenAI|ActionImport|ExecutionService|ProviderValidationService|GoogleAds.*Mutat|upload_to_openai|import_action|execute_live" .\backend\src\ai-data-pack\demo-seed
rg -n "deleteMany\\(\\{\\}\\)|dropDatabase|\\.drop\\(" .\backend\src\ai-data-pack\demo-seed
rg -n "NODE_ENV|ALLOW_DEMO_SEED|production|insertMany|deleteMany" .\backend\src\ai-data-pack\demo-seed\director-demo-seed.ts
rg -n 'status: "ENABLED"' .\backend\src\ai-data-pack\demo-seed\director-demo-seed.fixtures.ts
rg -n 'campaignBudgetId:.*campaignId|campaignBudgetId:.*adGroupId' .\backend\src\ai-data-pack\demo-seed\director-demo-seed.fixtures.ts
```

Results:

- Unit tests: pass, 5 tests.
- Dry-run CLI: pass with `NODE_ENV=test` and `ALLOW_DEMO_SEED=1`.
- Backend build: pass.
- External/mutation API grep: no matches.
- Broad delete/drop grep: no matches.
- Guard grep: confirms `NODE_ENV`, `ALLOW_DEMO_SEED`, insert, and delete points are in the CLI runner.
- Campaign `ENABLED` fixture grep: no matches.
- Campaign budget fallback grep: no matches.

Safety notes:

- Dry-run does not open a database connection.
- Apply/reset require `MONGODB_URI`.
- Apply always resets the demo id allowlist before insert.
- Reset deletes only deterministic demo ids generated by the large profile.
